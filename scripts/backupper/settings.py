import os

BASE_PATH = os.path.join()

#Targets included in zip
TARGETS = [os.path.join(BASE_PATH, 'data'), 
           os.path.join(BASE_PATH, 'pictures')]

BACKUP_DIRECTORY = os.path.join()
FILE_SUFFIX = "kuittipankki.zip"

LOG_PATH = os.path.join()

